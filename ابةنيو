local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local ActivateBtn = Instance.new("TextButton")

ScreenGui.Parent = game.CoreGui
MainFrame.Name = "Helixia_Lite"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.Position = UDim2.new(0.5, -100, 0.5, -50)
MainFrame.Size = UDim2.new(0, 200, 0, 100)
MainFrame.Active = true
MainFrame.Draggable = true

Title.Parent = MainFrame
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Text = "Helixia Reach"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.BackgroundColor3 = Color3.fromRGB(40, 40, 40)

ActivateBtn.Parent = MainFrame
ActivateBtn.Position = UDim2.new(0.1, 0, 0.45, 0)
ActivateBtn.Size = UDim2.new(0.8, 0, 0.4, 0)
ActivateBtn.Text = "ACTIVATE"
ActivateBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 0)
ActivateBtn.TextColor3 = Color3.fromRGB(255, 255, 255)

local function startReach()
    local reachValue = 20
    game:GetService("RunService").RenderStepped:Connect(function()
        for _, v in pairs(game.Workspace:GetChildren()) do
            if v.Name == "Ball" or v.Name == "Football" then
                local lp = game.Players.LocalPlayer
                if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
                    local mag = (lp.Character.HumanoidRootPart.Position - v.Position).Magnitude
                    if mag <= reachValue then
                        firetouchinterest(lp.Character.HumanoidRootPart, v, 0)
                        firetouchinterest(lp.Character.HumanoidRootPart, v, 1)
                    end
                end
            end
        end
    end)
end

ActivateBtn.MouseButton1Click:Connect(function()
    startReach()
    ActivateBtn.Text = "RUNNING"
    ActivateBtn.BackgroundColor3 = Color3.fromRGB(120, 0, 0)
end)
